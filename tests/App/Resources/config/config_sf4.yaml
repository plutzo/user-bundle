framework:
    session:
        storage_id: session.storage.mock_file

security:
    encoders:
        Marlinc\UserBundle\Model\UserInterface: plaintext

    firewalls:
        main:
            anonymous: true
